spring:
  application:
    name: code-challenge
  datasource:
    username:
      postgres
    password:
      postgres
    url:
      jdbc:postgresql://localhost:5432/challenge-db
    driver-class-name:
      org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto:
        validate
    show-sql:
      true